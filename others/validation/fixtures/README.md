# 本地外部对照 fixture 说明

本目录只保存能够合法随 DNAKit 分发、且不会复制受限第三方数值表或外部程序输出的固定验证输入。

旧 `primer3_reference.json` 已移除。当前验证器不会发现、安装、导入或执行 Primer3；Primer3 CLI adapter 使用测试时临时生成的受控替身验证协议、解析和资源边界。用户若进行真实 Primer3 科学验证，应在自己的许可环境中记录版本、命令、参数与输出摘要，不要把外部工具的完整资源文件复制进 DNAKit 发行包。
